json.array! @acts, partial: 'acts/act', as: :act

# json.sEcho params[:sEcho].to_i
# json.iTotalRecords @acts.total_count
# json.iTotalDisplayRecords @acts.total_count
# json.set! :aaData do
#   json.array! @acts, partial: 'show', as: :act
# end
