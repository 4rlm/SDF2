<div class="row">
  <% if activity.fav_sts != 'follow' && activity.fav_sts != 'hide' %>

    <!-- FOLLOW: TOGGLE STS PARTIAL -->
    <%= render partial: "activities/tog_fav_prt", locals: { activity: activity } %>

    <!-- HIDE: TOGGLE STS PARTIAL -->
    <%= render partial: "activities/tog_hide_prt", locals: { activity: activity } %>

  <% elsif activity.fav_sts == 'hide' %>

    <!-- FOLLOW: TOGGLE STS PARTIAL -->
    <%= render partial: "activities/tog_fav_prt", locals: { activity: activity } %>

    <!-- RESET: TOGGLE STS PARTIAL -->
    <%= render partial: "activities/tog_reset_prt", locals: { activity: activity } %>

  <% elsif activity.fav_sts == 'follow' %>

    <!-- RESET: TOGGLE STS PARTIAL -->
    <%= render partial: "activities/tog_reset_prt", locals: { activity: activity } %>

    <!-- HIDE: TOGGLE STS PARTIAL -->
    <%= render partial: "activities/tog_hide_prt", locals: { activity: activity } %>

  <% end %>
</div>
