<div class="col-md-4">
  <%= link_to toggle_sts_activities_path(id: activity.id, fav_sts: 'none'), class: "btn btn-default btn-xs", remote: true do %>
    <%= icon('fas', 'sync')%> Reset
  <% end %>
</div>
