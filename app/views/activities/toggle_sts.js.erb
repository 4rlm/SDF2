// $("#yield-content").html("<%= j (render 'toggle_sts', activity: @activity) %>");
// $("#yield-toggle-sts").html("<%= j (render 'toggle_sts', activity: @activity) %>");

$("#yield-toggle-web_activity").html("<%= j (render 'toggle_sts', activity: @activity) %>");
